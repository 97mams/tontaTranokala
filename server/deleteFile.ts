function fafanaTy() {
    // eto no manoratra
    return " at objet request=)creation.php artisan: name migration puble function up word.schema:create c postr;function (blueprint! and trible )  s table =)rdc ;s table=) string (tible);s table sting (slug) =)unique (); s table=) long teet(content); php artisan migrate;creat model php a makes model name model"
}